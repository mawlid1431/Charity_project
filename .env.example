# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Your Supabase project URL
VITE_SUPABASE_URL=your_supabase_url_here

# Supabase Anonymous Key (safe for client-side)
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Service Role Key (NEVER expose this in client-side code!)
# Only use this for server-side operations or admin functions
VITE_SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================
# ADMIN CREDENTIALS (SECURITY SENSITIVE)
# ============================================
# Admin login email - used for dashboard access
VITE_ADMIN_EMAIL=admin@yourdomain.com

# Admin login password - keep this secure!
VITE_ADMIN_PASSWORD=your_secure_password_here

# ============================================
# SECURITY NOTES
# ============================================
# 1. NEVER commit the .env file to version control
# 2. Add .env to your .gitignore file
# 3. Use different credentials for production
# 4. Rotate passwords regularly
# 5. Consider using proper authentication (OAuth, JWT) for production
